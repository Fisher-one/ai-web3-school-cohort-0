# 2026-05-21 学习计划 — Week 1 Day 4

## 📊 当前状态

- 已提交：12/37 任务（185 分）
- 可用任务：21 个
- 不可用：4 个（回放类，实时参加版本已提交导致互斥）
- 昨天：搞定了 Web3 运行原理直播 + Co-learning，Agent API 已接通

---

## ⚡ 今日直播

| 时间 | 活动 | 积分 | 截止 |
|------|------|------|------|
| **5.21 19:30** | AI 下乡计划｜Web3 应用 | 20 pts（实时） | 5/23 13:00 |
| 回放 | 同上 | 10 pts | 5/24 15:59 |

⚠️ 实时参加和回放互斥，只能交一个。建议实时参加拿 20 pts。

WCB 活动页：https://web3career.build/zh/programs/AI-Web3-School#tab=learning

---

## 📖 Handbook 阅读（今天重点章节）

按顺序读，不要跳：

| 顺序 | 章节 | 链接 | 关联任务 |
|------|------|------|----------|
| 1 | RAG（检索增强生成） | https://aiweb3.school/zh/handbook/ai/rag/ | → AI 概念卡片、AI 可交互产物 |
| 2 | Agent（智能体） | https://aiweb3.school/zh/handbook/ai/agent/ | → 受限 Web3 助手、流程图 |
| 3 | Smart Contract（智能合约） | https://aiweb3.school/zh/handbook/web3/smart-contract/ | → 测试网交易、最小合约部署 |
| 4 | Account Abstraction（账户抽象） | https://aiweb3.school/zh/handbook/web3/account-abstraction/ | → EOA/智能账户/多签对比 |

**阅读逻辑**：上午 AI 侧（RAG + Agent），下午 Web3 侧（Smart Contract + AA）。你昨天读了 Prompt + Context，今天 RAG + Agent 把 AI 基础补完。

---

## ✅ 最小路径（今天搞定，预计 2.5h）

- [x] **19:30 实时参加直播**｜AI 下乡计划（20 pts）— ✅ 已提交
- [ ] **读 RAG 章节**（30 min）— 搞清楚为什么 context window 不够用、RAG 怎么解决
- [ ] **读 Agent 章节**（30 min）— Agent 循环、工具调用、和普通 LLM 聊天的区别
- [ ] **AI 基础概念卡片**（10 pts，30 min）— 用自己话写 LLM/Prompt/Context/RAG/Agent 五个概念
- [ ] **AI × Web3 行业信息流关注清单**（20 pts，20 min）— 列 5-8 个你常看/打算看的源

---

## 📈 推荐路径（有精力就做，+30 min）

- [ ] **读 Smart Contract 章节**（20 min）— 你 Web3 有底子，扫一遍就行
- [ ] **读 Account Abstraction 章节**（20 min）— EOA vs 智能账户，这块你以后做 Agent Wallet 会用到
- [ ] **Web3 基础概念卡片**（10 pts，15 min）— 密码学/钱包/合约/账户抽象，你熟，很快
- [ ] **完成一笔测试网转账**（20 pts，20 min）— MetaMask + Sepolia faucet，昨天没做的补上

---

## 🚀 挑战路径（选做）

- [ ] **画出 AI × Web3 最小交叉流程图**（30 pts）— 把"用户提问 → Agent 推理 → 链上执行"画清楚
- [ ] **部署或调用一个最小智能合约**（30 pts）— Remix IDE 就行，别搞 Foundry，先跑通
- [ ] **比较 EOA、智能账户、多签的权限差异**（30 pts）— 表格对比：谁控制、怎么恢复、gas 谁付

---

## 🔔 本周剩余活动预告（别漏了）

| 日期 | 活动 | 实时分 | 截止 |
|------|------|--------|------|
| 5.22 | Z.AI | 20 pts | 5/24 12:00 |
| 5.22 | Week 1 例会 | 20 pts | 5/24 13:00 |
| 5.23 | Open Agentic Economy | 20 pts | 5/24 15:59 |
| 5.23 | Co-learning | 20 pts | 5/24 15:59 |

**本周是活动密集期**，22-23 号每天两场，周五（23 号）前一批 deadline 到期。能实时参加就实时参加，拿的分多。

---

## 学习笔记

### RAG（检索增强生成）

**为什么需要 RAG**：LLM 自己会瞎编（幻觉），RAG 让它回答有来源可查、有有效期的信息。

**Chunk（文本切片）**：
把大文档切成小块，但切法有讲究 — 切太大语义模糊，切太小上下文断。稳的做法是按结构切：title → description → content，每个 chunk 记录来源、时间、版本。

**Embedding（嵌入模型）**：
谁把文本转成向量的？Embedding 模型（比如 OpenAI text-embedding-3）。用户问题也得先走 embedding 才能在向量库里搜。不同模型算出的向量不能混用。

**Vector DB（向量数据库）**：
存这些 chunk 的地方。不是普通数据库，是把文本转成向量后存，方便语义检索 — "怎么部署合约"能找到听起来像的 chunk，不是关键词匹配。

**Retriever（检索器）**：
从向量库里根据用户问题拿相关 chunk。暂时还没完全搞懂它和 Vector DB 的具体交互细节，后续动手用一下再补。

**Rerank（重排序）**：
对检索结果再做一次筛选排序，兜底检查。但加了会增加延迟，看场景决定要不要用。

**Citation（引用）**：
回复里标注信息来源 — "这段话来自 Handbook 第三章"，用户能追溯。

**完整 RAG 链路（Agent 补充）**：
用户问题 → embedding 模型转向量 → 向量库检索 top-k chunk →（可选 rerank）→ 把 chunk 塞进 prompt → LLM 生成答案 → 标注 citation。关键：LLM 回答里不含原文 chunk，citation 是后加的。

---

### Agent（智能体）

**LLM vs Agent**：LLM 是会说话的底座，Agent 是会干活的人。Agent 能拆解任务、调工具、写代码、改配置、发请求。

**ReAct 模式（Agent 补充）**：大部分 agent 框架底层都这套 — Think（推理下一步）→ Act（调工具）→ Observe（看结果）→ 循环。Planning / Tool Use / Reflection 不是三件事，是同一个循环里的不同环节。

**Tool Use（工具调用）**：
本质调 API 再封装。比如"查天气"工具，Agent 调的是天气 API。做过后端天天写这个，能理解。

**Planning（任务规划）**：
把模糊需求拆成可执行步骤。没用过，但感觉得设规则 — 某步失败怎么办，要不要回退，用户没确认前别替我执行。

**State（状态管理）**：
任务执行中的各种状态 — 调用中、成功、失败、超时，类似后台任务状态机。

**Reflection（自我检查）**：
中间件，让 Agent 执行完自己检查一遍结果。没用过，待验证。

**Multi-Agent（多智能体）**：
多个 Agent 各管一块。好处是专注，问题是出错链式放大 — A 给了错结果，B 可能把错的当真需求往下做。

**Memory 分层（Agent 补充）**：
Agent 不是每次都从零开始。短期记忆 = 当前对话上下文，长期记忆 = 用向量库存历史交互。不然重启一次对话全忘了。

**Context window 天花板（Agent 补充）**：
Agent 每次调工具、读结果，context 越塞越多，会爆。实操里这是最大的坑，不是概念问题。

**Agent 使用边界（我自己的判断）**：
Agent 能写能读能改配置能发请求，不能完全依赖。得画红线：
- 不确定的事直接说不知道，别编
- 涉及钱/权限的操作必须确认
- 不要替我决定

---

### Smart Contract（智能合约）

**定义**：链上交易可以通过代码自动执行。用 Solidity 写，主要跑在 EVM（以太坊虚拟机）上。

**ABI**：合约结构说明书，类似 API 文档给前端看的 — 有哪些方法、参数什么类型。

**Event**：链上日志。比如转账成功 emit Transfer 事件，前端/索引器能监听。

**Gas 机制（Agent 补充）**：
代码在 EVM 上跑，每一步都要付 gas。循环、存储写入贵，读便宜。写合约不光是功能对，还得省 gas。

**Storage vs Memory（Agent 补充）**：
Solidity 里变量存 storage（永久，贵）还是 memory（临时，便宜），搞错一个是 bug 也是浪费 gas。

**安全基础（Agent 补充）**：
写合约之前至少知道重入攻击、整数溢出。跟后面「部署或调用最小合约」任务直接相关。

**合约升级**：几种方案还没做过，需要补。

**一笔合约调用的完整流程（抄文档，备查）**：
1. 前端读钱包地址和当前网络
2. 前端根据 ABI 编码调用数据（比如 vote(proposalId)）
3. 钱包展示交易信息，用户确认签名
4. 交易广播到 RPC 节点
5. 验证者打包进区块
6. EVM 执行合约逻辑，成功 → 更新状态 + emit event，失败 → revert
7. 前端监听交易回执，更新页面
8. 索引器读 event，更新历史记录/看板

---

### Account Abstraction（账户抽象）

**传统钱包的问题**（用后端经验类比）：

EOA（外部账户）= 裸数据库连接，私钥就是连接串，丢了全完。每次操作都得手动签名 = 每次数据库操作弹确认框。

**AA 解决的问题**：
- 智能账户 = 给钱包加个中间件层
- 可以写规则："每天转账上限 0.1 ETH，超过拒绝"
- 可以多签："超过 500U 需要两个人确认"
- 可以用 USDC 付 gas，不用非得有 ETH
- 私钥丢了能通过守护者恢复

**ERC-4337 骨架（不展开细节）**：
- 不修改以太坊底层协议，加了一层 UserOperation
- EntryPoint 合约统一处理所有智能账户的交易
- Paymaster 合约可以帮用户付 gas（或让用户用其他代币付）
- 流程：用户发起操作 → 打包成 UserOperation → 发给 Bundler → Bundler 调 EntryPoint → EntryPoint 调你的智能账户

---

## Agent 补充点（Fisher 原笔记遗漏的）

| 模块 | 遗漏 | 重要性 |
|------|------|--------|
| RAG | **Embedding 模型**：谁把文字转成向量 | 入口环节，不问不提会漏 |
| RAG | **完整链路**：零件串起来的流程 | 5 个零件你都讲了但没串 |
| Agent | **ReAct 模式**：Think → Act → Observe 循环 | Planning/Tool Use/Reflection 的本质 |
| Agent | **Memory 分层**：短期 vs 长期记忆 | 跟日常用 Hermes 直接相关 |
| Agent | **Context window 天花板** | 实操最大坑 |
| 智能合约 | **Gas 机制**：读便宜写贵 | 写合约绕不开 |
| 智能合约 | **Storage vs Memory** | bug + 浪费 gas 的来源 |
| 智能合约 | **安全基础**（重入、溢出） | 跟后续任务直接挂钩 |
| AA | **ERC-4337 实现**：UserOperation / Bundler / EntryPoint / Paymaster | 知道骨架就不用怕了 |

---

## 遇到的问题

<!-- 卡在哪了、什么没看懂、什么跑不通 -->

---

## 打卡内容草稿
## 打卡内容草稿

**Day 4 打卡｜Handbook 四章：RAG / Agent / 智能合约 / 账户抽象**

今天把 Handbook 四章啃完了，信息量很大。不记下来下次还得重看。

---

**RAG（检索增强生成）**

之前只知道 RAG 能让 LLM 查外部知识，今天把里面的零件拆开看了一遍：

Embedding 模型把文字转成向量，用户问题也一样要走 embedding。不同模型算出来的向量不能混用。

Chunk 切片有讲究 — 切太大语义糊，切太小上下文断。稳的做法是按结构切：title → description → content，每个 chunk 记来源、时间、版本。

向量库存的不是原文是向量，检索靠语义不是关键词。Retriever 从库里拿相关 chunk，但具体怎么拿的还没完全搞懂，后续动手用一下再补。Rerank 再筛一轮兜底，但加了会有延迟。Citation 标来源，用户能追溯。

完整链路：用户问题 → embedding → 向量库检索 top-k → （可选 rerank）→ chunk 塞进 prompt → LLM 生成 → citation。LLM 回答里不含原文，citation 是后加的。

---

**Agent（智能体）**

LLM 是会说话的底座，Agent 是会干活的人。底层是 ReAct 模式：Think（推理下一步）→ Act（调工具）→ Observe（看结果）→ 循环。Planning、Tool Use、Reflection 不是三件事，是同一个循环的不同环节。

Tool Use 本质就是调 API 再封装，因为我做后端，这我能理解。

Multi-Agent 各管一块，问题是出错链式放大 — A 给错结果，B 可能把错的当真需求往下做。

Memory 有分层：短期 = 当前对话上下文，长期 = 向量库存历史交互。Context window 会越塞越多，实操里这是最大坑。

Agent 得画红线：不确定的事直接说不知道别编，涉及钱/权限必须确认，不要替我决定。

---

**智能合约**

链上交易用代码自动执行，Solidity 写，跑 EVM 上。

ABI 就是合约的 API 文档给前端看。Event 是链上日志，前端/索引器能监听。

Gas 每步都要付 — 存储写入贵，读便宜。Solidity 里变量存 storage（永久贵）还是 memory（临时便宜），搞错一个是 bug 也是浪费 gas。

写合约之前至少得知道重入攻击、整数溢出。合约升级几种方案还没做，后续补。

一笔合约调用的完整流程（抄文档备查）：前端读钱包和网络 → 按 ABI 编码调用 → 钱包让用户签名 → 广播到 RPC → 验证者打包 → EVM 执行，成功更新状态 + emit event，失败 revert → 前端等回执更新页面 → 索引器读 event 更新看板。

---

**Account Abstraction（账户抽象）**

这章第一遍没看懂，Agent 帮我重新梳理了一遍：

传统钱包（EOA）像裸数据库连接，私钥丢了全完，每次操作还得手动签名。AA 就是给钱包加个中间件层 — 可以写规则限制转账额度、多签确认、用 USDC 付 gas、私钥丢了能通过守护者恢复。

ERC-4337 不修改以太坊底层，加了 UserOperation 这一层。EntryPoint 统一处理智能账户交易，Paymaster 帮用户付 gas。流程：操作 → UserOperation → Bundler → EntryPoint → 智能账户执行。

---

**今天没搞懂的：**

- Retriever 和 Vector DB 具体交互细节
- Rerank 延迟到底多大
- 合约升级的几种方案

后续动手补。

#AIWeb3School #Week1Day4

---

## 打卡链接

<!-- 提交后把链接贴这里 -->

---

> 白天把 RAG + Agent 读完，概念卡片和信息流清单做了。晚上 7:30 别断直播。Web3 侧章节你有底子，扫得快——今天重点是把 AI 基础那五个概念的连贯性搞明白：模型怎么理解世界 → Prompt 怎么引导 → Context 怎么管理 → RAG 怎么扩展 → Agent 怎么执行。
